# Password for the Reolink user you created for Frigate.
# config.yml refers to this as {FRIGATE_RTSP_PASSWORD} so the password is
# never written into the config file itself.
FRIGATE_RTSP_PASSWORD=your_camera_password
