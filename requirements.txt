paho-mqtt>=2.0
httpx>=0.27
# Headless build: this runs as a service, and the GUI variant drags in libGL
# etc. which are not present in a slim container or on a headless Pi.
opencv-python-headless>=4.9
numpy>=1.26
anthropic>=0.40
supabase>=2.4
python-dotenv>=1.0
